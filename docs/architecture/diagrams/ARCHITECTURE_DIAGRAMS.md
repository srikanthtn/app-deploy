# System Architecture Diagrams

## 1. Hexagonal Architecture Overview

```
                          ┌─────────────────────────────────┐
                          │      API Layer (FastAPI)        │
                          │  - HTTP Request/Response        │
                          │  - Authentication               │
                          │  - Validation (Pydantic)        │
                          └────────────┬────────────────────┘
                                       │
                                       ▼
                          ┌─────────────────────────────────┐
                          │   Application Layer             │
                          │  - Use Cases (Orchestration)    │
                          │  - AnalyzeCleanlinessUseCase    │
                          └─────┬─────────┬─────────┬───────┘
                                │         │         │
                ┌───────────────┘         │         └───────────────┐
                ▼                         ▼                         ▼
    ┌──────────────────┐    ┌──────────────────────┐   ┌──────────────────┐
    │ VisionProvider   │    │  StorageProvider     │   │ AuditRepository  │
    │     (Port)       │    │      (Port)          │   │     (Port)       │
    └─────────┬────────┘    └──────────┬───────────┘   └────────┬─────────┘
              │                        │                         │
    ┌─────────▼─────────┐   ┌─────────▼──────────┐   ┌─────────▼─────────┐
    │  Infrastructure   │   │  Infrastructure    │   │  Infrastructure   │
    │  RekognitionAdapter│   │    S3Adapter       │   │ DynamoDBRepository│
    │  TFLiteAdapter    │   │                    │   │                   │
    └─────────┬─────────┘   └──────────┬─────────┘   └─────────┬─────────┘
              │                        │                         │
    ┌─────────▼─────────┐   ┌─────────▼──────────┐   ┌─────────▼─────────┐
    │  Amazon           │   │   Amazon S3        │   │  Amazon DynamoDB  │
    │  Rekognition      │   │                    │   │                   │
    └───────────────────┘   └────────────────────┘   └───────────────────┘

                          ┌─────────────────────────────────┐
                          │       Domain Layer (Core)       │
                          │  - Entities (AuditResult)       │
                          │  - Value Objects (Status)       │
                          │  - Services (Evaluator)         │
                          │  - ZERO external dependencies   │
                          └─────────────────────────────────┘
```

**Key**: 
- Ports (interfaces) = White boxes
- Adapters (implementations) = Gray boxes  
- External services = Dark boxes
- Domain = Center (no arrows pointing out!)

---

## 2. Request Flow Diagram

```
┌─────────┐
│ Mobile  │
│   App   │
└────┬────┘
     │ 1. POST /api/v1/hygiene/analyze
     │    (multipart: dealer_id, checkpoint_id, image)
     ▼
┌──────────────────────────────────────────────────────┐
│                  API Layer                           │
│  ┌──────────────────────────────────────────────┐   │
│  │ 1. Validate request (Pydantic)               │   │
│  │ 2. Authenticate user (JWT token)             │   │
│  │ 3. Inject correlation ID                     │   │
│  └────────────────┬─────────────────────────────┘   │
└───────────────────┼──────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────┐
│            Application Layer                         │
│  ┌──────────────────────────────────────────────┐   │
│  │ AnalyzeCleanlinessUseCase.execute()          │   │
│  │                                               │   │
│  │ Step 1: Create ImageMetadata                 │   │
│  │ Step 2: Upload to S3 ───────────────┐        │   │
│  │ Step 3: Analyze vision ──────────┐  │        │   │
│  │ Step 4: Evaluate (domain) ─┐     │  │        │   │
│  │ Step 5: Save audit ────┐   │     │  │        │   │
│  │ Step 6: Return result  │   │     │  │        │   │
│  └────────────────────────┼───┼─────┼──┼────────┘   │
└───────────────────────────┼───┼─────┼──┼─────────────┘
                            │   │     │  │
           ┌────────────────┘   │     │  │
           │  ┌─────────────────┘     │  │
           │  │  ┌────────────────────┘  │
           │  │  │  ┌──────────────────--┘
           ▼  ▼  ▼  ▼
┌──────────────────────────────────────────────────────┐
│            Infrastructure Layer                      │
│  ┌─────────────────┐  ┌──────────────────────────┐  │
│  │ S3Adapter       │  │ RekognitionAdapter       │  │
│  │ .upload_image() │  │ .analyze_image_from_s3() │  │
│  └────────┬────────┘  └──────────┬───────────────┘  │
└───────────┼──────────────────────┼───────────────────┘
            │                      │
            ▼                      ▼
    ┌──────────────┐      ┌──────────────┐
    │   Amazon S3  │      │  Rekognition │
    │ PUT /image   │      │ DetectLabels │
    └──────┬───────┘      └──────┬───────┘
           │                     │
           │ Returns:            │ Returns:
           │ StorageMetadata     │ Labels[]
           │                     │
           └──────────┬──────────┘
                      ▼
        ┌──────────────────────────────┐
        │  Domain Layer                │
        │  CleanlinessEvaluator        │
        │  .evaluate()                 │
        │                              │
        │  Business Rules:             │
        │  - Check negative labels     │
        │  - Check confidence          │
        │  - Apply manual override     │
        │  - Generate explainability   │
        └──────────────┬───────────────┘
                       │
                       │ Returns: AuditResult
                       ▼
        ┌──────────────────────────────┐
        │  Infrastructure              │
        │  AuditRepository.save()      │
        │  → DynamoDB PutItem          │
        └──────────────┬───────────────┘
                       │
                       ▼
                  ┌─────────┐
                  │ Response│
                  │   JSON  │
                  └────┬────┘
                       │
                       ▼
                  ┌─────────┐
                  │ Mobile  │
                  │   App   │
                  └─────────┘
```

**Timings**:
1. S3 Upload: ~200ms
2. Rekognition: ~800ms
3. Domain Evaluation: <10ms
4. DynamoDB Write: ~50ms
**Total**: ~1.1s (well under 2s SLA)

---

## 3. Dependency Injection Flow

```
                    main.py
                       │
                       ▼
            ┌──────────────────────┐
            │   app = create_app() │
            └──────────┬───────────┘
                       │
          ┌────────────┴────────────┐
          │                         │
          ▼                         ▼
    ┌─────────────┐         ┌──────────────┐
    │ Middleware  │         │   Routes     │
    │ Registration│         │ Registration │
    └─────────────┘         └──────┬───────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
                    ▼                             ▼
         ┌─────────────────────┐      ┌─────────────────────┐
         │ dependencies.py     │      │  @router.post(...)  │
         │                     │      │                     │
         │ get_vision_provider()│◄─────┤  Depends(...)      │
         │ get_storage_provider()       │                     │
         │ get_audit_repository()       │                     │
         │ get_analyze_use_case()       │                     │
         └────────┬────────────┘      └─────────────────────┘
                  │
    ┌─────────────┼──────────────┐
    │             │              │
    ▼             ▼              ▼
┌─────────┐  ┌─────────┐  ┌──────────┐
│Rekogn.  │  │   S3    │  │ DynamoDB │
│Adapter  │  │ Adapter │  │Repository│
└────┬────┘  └────┬────┘  └────┬─────┘
     │            │            │
     └────────────┴────────────┘
                  │
                  ▼
        ┌─────────────────────┐
        │AnalyzeCleanlinessUse│
        │       Case           │
        │ (All deps injected)  │
        └─────────────────────┘
```

**Key Points**:
- Dependencies resolved at startup (or request time with `@lru_cache`)
- Use cases receive fully configured adapters
- Easy to swap implementations (env vars)
- Testable with mocks (just override dependencies)

---

## 4. Testing Strategy

```
                    Code Coverage
                         │
        ┌────────────────┼────────────────┐
        │                │                │
   Unit Tests      Integration      End-to-End
     (70%)          Tests (20%)      Tests (10%)
        │                │                │
        ▼                ▼                ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ Domain Layer │  │Infrastructure│  │  Full API    │
│              │  │    Layer     │  │   Workflow   │
│ - Entities   │  │              │  │              │
│ - Value Objs │  │ - Rekognition│  │ POST /analyze│
│ - Services   │  │   Adapter    │  │ GET /audits  │
│              │  │   (with moto)│  │              │
│ NO external  │  │              │  │ TestClient   │
│ dependencies │  │ - S3 Adapter │  │ (FastAPI)    │
│              │  │   (with moto)│  │              │
│ Instant      │  │              │  │              │
│ execution    │  │ Medium speed │  │ Slower       │
│ < 1s         │  │ < 10s        │  │ < 60s        │
└──────────────┘  └──────────────┘  └──────────────┘
```

**Philosophy**: 
- Most tests = Domain (fast, reliable)
- Some tests = Infrastructure (mocked AWS)
- Few tests = E2E (expensive, slower)

---

## 5. Extensibility: Adding New Vision Provider

```
Current State:
┌──────────────────┐
│ VisionProvider   │ ◄─── Domain Port (interface)
│    (Port)        │
└────────┬─────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌─────────┐ ┌──────────┐
│Rekogn.  │ │ TFLite   │
│Adapter  │ │ Adapter  │
└─────────┘ └──────────┘

Adding Custom Model:
┌──────────────────┐
│ VisionProvider   │
│    (Port)        │
└────────┬─────────┘
         │
    ┌────┴─────────┬────────┐
    ▼              ▼        ▼
┌─────────┐ ┌──────────┐ ┌──────────┐
│Rekogn.  │ │ TFLite   │ │ Custom   │  ◄─── NEW
│Adapter  │ │ Adapter  │ │ Adapter  │
└─────────┘ └──────────┘ └─────┬────┘
                                │
                                ▼
                         ┌────────────┐
                         │ PyTorch    │
                         │ Model      │
                         └────────────┘

Steps:
1. Create CustomAdapter(VisionProvider)  ✓ Same interface!
2. Implement analyze_image()             ✓ Load model
3. Update dependencies.py                ✓ Add factory case
4. Set env: VISION_PROVIDER=custom       ✓ Done!

👉 ZERO changes to domain/application layers!
```

---

## 6. Cost Optimization Strategy

```
                Analysis Request
                      │
                      ▼
           ┌──────────────────┐
           │  Check Cache?    │ ◄───┐
           └────┬────────┬────┘     │
                │        │          │
           No   │        │ Yes      │
                ▼        ▼          │
        ┌──────────┐  Return        │
        │Processing│  Cached        │
        │ Strategy │  Result        │
        └────┬─────┘                │
             │                      │
    ┌────────┼────────┐             │
    │        │        │             │
Standard   Premium  Custom          │
(TFLite)   (Rekogn) (PyTorch)       │
$0         $0.001   $0.0005         │
    │        │        │             │
    └────────┼────────┘             │
             │                      │
             ▼                      │
      ┌───────────┐                 │
      │  Cache    │─────────────────┘
      │  Result   │ TTL: 7 days
      └───────────┘

Cost Comparison (1M images/month):
- 100% Rekognition: $1,000/month
- 80% TFLite, 20% Rekognition: $200/month (80% savings!)
- With caching: $100/month (90% savings!)
```

---

## 7. Error Handling Flow

```
                 Request
                    │
                    ▼
          ┌─────────────────┐
          │  API Layer      │
          │  Try/Catch      │
          └────┬────────────┘
               │
        ┌──────▼───────┐
        │ Use Case     │
        │ Execute()    │
        └──┬────────┬──┘
           │        │
    Success│        │Error
           │        │
           ▼        ▼
        Return   Exception
        Result      │
                    │
         ┌──────────┴───────────────┐
         │                          │
    Domain Exception         Infrastructure
    (Business Error)           Exception
         │                          │
         ▼                          ▼
┌────────────────┐        ┌────────────────┐
│ InvalidImage   │        │ S3Error        │
│ AlreadyFinalized│        │ RekognitionErr │
│ etc.           │        │ NetworkError   │
└────────┬───────┘        └────────┬───────┘
         │                         │
         └─────────┬───────────────┘
                   │
                   ▼
         ┌─────────────────┐
         │  API Layer      │
         │  Translate to   │
         │  HTTP Status    │
         └────┬────────────┘
              │
    ┌─────────┴─────────┐
    │                   │
    ▼                   ▼
400 Bad Request    500 Internal
(Client error)     Server Error
```

**Key**: Domain exceptions = 400, Infrastructure exceptions = 500

---

## 8. Production Deployment Architecture

```
                    ┌─────────────┐
                    │   Route53   │ (DNS)
                    └──────┬──────┘
                           │
                           ▼
                    ┌─────────────┐
                    │ CloudFront  │ (CDN + DDoS)
                    └──────┬──────┘
                           │
                           ▼
                    ┌─────────────┐
                    │   WAF       │ (Firewall)
                    └──────┬──────┘
                           │
                           ▼
           ┌───────────────────────────┐
           │    API Gateway            │
           │    (Rate Limiting)        │
           └───────┬───────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
        ▼                     ▼
┌──────────────┐      ┌──────────────┐
│  Lambda      │      │  ECS Fargate │
│  (Serverless)│  OR  │  (Container) │
└──────┬───────┘      └──────┬───────┘
       │                     │
       └──────────┬──────────┘
                  │
      ┌───────────┴───────────┐
      │                       │
      ▼                       ▼
┌─────────────┐       ┌──────────────┐
│ Rekognition │       │     S3       │
│             │       │ (Versioned,  │
│             │       │  Encrypted)  │
└─────────────┘       └──────────────┘
                              │
                              ▼
                      ┌──────────────┐
                      │  DynamoDB    │
                      │  (Audit Log) │
                      └──────────────┘
                              │
                              ▼
                      ┌──────────────┐
                      │  CloudWatch  │
                      │  (Metrics +  │
                      │   Logs)      │
                      └──────────────┘
```

**High Availability**: Multi-AZ, auto-scaling, health checks

---

These diagrams illustrate the complete architecture from multiple perspectives!
